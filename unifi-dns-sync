#!/usr/bin/env python3
"""
Unifi DNS Synchronization Script

Entry point for the unifi-dns-sync application.
"""

import sys
import os

# Add the src directory to the Python path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from unifi_dns_sync.cli import main

if __name__ == "__main__":
    main()
